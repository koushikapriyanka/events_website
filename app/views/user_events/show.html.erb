<p id="notice"><%= notice %></p>
<%= form_with(url: cancel_event_user_event_path, method: "patch") do |form| %>
  <% if @user_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_event.errors.count, "error") %> prohibited this @user_event from being saved:</h2>

      <ul>
      <% @user_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event %>
    <%= form.label @user_event.event.name, disabled: true%> 
  </div>

  <div class="field">
    <%= form.label :ticket_fee %>
    <%= form.label @user_event.ticket_fee, disabled: true%> 
  </div>

  <div class="field">
    <%= form.hidden_field :id, value: @user_event.id%>
  </div>

  <%if @user_event.present? && @user_event.can_unattend?%>
    <div class="actions">
      <%= form.submit 'Unattend'%>
    </div>
  <% end %>
<% end %>
